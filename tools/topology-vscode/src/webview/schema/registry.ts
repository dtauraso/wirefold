// Minimal re-export shim: schema/node-types.ts imports NODE_DEFS from here.
// RF_NODE_TYPES (React Flow node component map) is no longer needed — the 2D RF view is removed.
export { NODE_DEFS } from "./node-defs";
